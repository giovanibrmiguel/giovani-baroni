---
/**
 * GrainOverlay
 * Applies a subtle film grain texture over the entire page.
 * Fixed position, pointer-events: none — completely decorative.
 * Opacity controlled by --grain-opacity CSS variable in global.css.
 */
---

<div class="grain-overlay" aria-hidden="true"></div>
